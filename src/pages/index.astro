---
import Welcome from "../components/Welcome.astro";
import Layout from "../layouts/Layout.astro";
import { loadSession, protectRoute } from "../middleware";

// Upewniamy się, że ta strona jest zawsze renderowana na serwerze
export const prerender = false;

// Używamy istniejącego middleware do obsługi sesji
export const onRequest = loadSession;
---

<Layout>
  <Welcome />
</Layout>